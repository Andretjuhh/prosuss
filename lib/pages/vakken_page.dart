import 'package:flutter/material.dart';

class VakkenPage extends StatelessWidget {
  const VakkenPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Vakken', style: TextStyle(fontSize: 30),),
    );
  }
}
