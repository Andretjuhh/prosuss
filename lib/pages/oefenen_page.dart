import 'package:flutter/material.dart';

class OefenenPage extends StatelessWidget {
  const OefenenPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Oefenen', style: TextStyle(fontSize: 30),),
    );
  }
}
