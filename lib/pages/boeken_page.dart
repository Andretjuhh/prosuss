import 'package:flutter/material.dart';

class BoekenPage extends StatelessWidget {
  const BoekenPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Boeken', style: TextStyle(fontSize: 30),),
    );
  }
}
