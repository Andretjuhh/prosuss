import 'package:flutter/material.dart';

class VoortgangPage extends StatelessWidget {
  const VoortgangPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Voortgang', style: TextStyle(fontSize: 30),),
    );
  }
}

